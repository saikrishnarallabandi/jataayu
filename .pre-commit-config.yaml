# Mirrors what CI already enforces (.github/workflows/test.yml), so a failure
# surfaces before the push rather than after.
repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.4
    hooks:
      - id: ruff
        args: [--fix]
        files: ^jataayu/
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: [--maxkb=1024]   # bulk corpora and weights belong outside git
      - id: check-merge-conflict
      - id: detect-private-key
