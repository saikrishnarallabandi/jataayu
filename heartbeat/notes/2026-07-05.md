# Jataayu Eval Trust Pass — 2026-07-05

## Current Read

Active heartbeat priority is Jataayu evals: find baseline-fails / Jataayu-saves cases and fix tool-call JSON before trusting AgentDojo.

The current AgentDojo smoke result at `eval/results/agentdojo_workspace_qwen35b_smoke.json` is not decision-grade:

- Model: `qwen3.6:35b-a3b`
- Suite: `workspace`
- Attack: `important_instructions`
- Rows: 1 baseline-only case
- Utility without attack: `0`
- Utility under attack: `0`
- Attack success rate: `0`

That means the run proves almost nothing about the defense. If the baseline cannot complete the clean user task, the zero ASR may just be task/model/tool-call failure, not successful protection.

## Next Finishable Slice

Run a small baseline-only calibration first and keep only tasks where utility is non-zero. Then compare those exact tasks with Jataayu enabled.

Candidate command shape:

```bash
python eval/agentdojo/run_agentdojo.py \
  --model local \
  --model-id qwen3.6:35b-a3b \
  --local-base-url http://127.0.0.1:11434/v1 \
  --suite workspace \
  --attack important_instructions \
  --user-tasks user_task_0 user_task_1 user_task_2 user_task_3 user_task_4 \
  --variants baseline \
  --out eval/results/agentdojo_workspace_qwen35b_baseline_calibration.json
```

If at least two tasks show clean utility, rerun the same list with:

```bash
python eval/agentdojo/run_agentdojo.py \
  --model local \
  --model-id qwen3.6:35b-a3b \
  --local-base-url http://127.0.0.1:11434/v1 \
  --suite workspace \
  --attack important_instructions \
  --user-tasks <only-clean-utility-task-ids> \
  --variants baseline jataayu \
  --min-status MEDIUM \
  --out eval/results/agentdojo_workspace_qwen35b_jataayu_slice.json
```

## Risk

Do not interpret aggregate ASR until clean-task utility is non-zero. The first failure to debug is likely local model tool-call formatting, not Jataayu itself.
