__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
eval/
downloads/
runs/
eggs/
.eggs/
lib/
lib64/
parts/
training/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST
.env
.venv
env/
venv/
ENV/
.pytest_cache/
.ruff_cache/
*.pyc
.DS_Store

# Layer 2 training artifacts (generated)
training/secalign/data/
training/**/exp/
exp/

# session working notes
tasks/

# eval run artifacts (trajectory logs + transcripts)
runs/
eval/results/*.log
eval/eval/

# model weights & training data artifacts (large; regenerable, not for the repo
# or the RELAI simulator workspace)
*.safetensors
*.bin
*.gguf
*.pt
*.pth
*.ckpt
*.onnx
adapter_model.*
adapter_config.json
training/**/data/*.jsonl
training/**/exp/
training/**/checkpoints/

# RELAI local files
.relai/config.toml
.relai/.npmrc
.relai/node_modules/
.relai/runs/
.relai/optimizer_runs/
.relai/memory
.relai/optimizer-state/
.relai/optimizer-scope.json
.relai/simulator.env
.relai/tag-records/

# RELAI run artifacts + benchmark exports — 239MB / 17k files in the working tree.
# Regenerable; purged from history 2026-07-11. Blanket-ignored (the entries above were partial).
.relai/
